# Core dependencies
typer>=0.12.0
pydantic>=2.0.0
python-dotenv>=1.0.0
anthropic>=0.40.0
rich>=13.0.0

# Speech Recognition (macOS native)
SpeechRecognition>=3.10.0
pyaudio>=0.2.13

# Optional: for future enhancements
# faster-whisper>=0.10.0  # For Whisper ASR (offline, higher accuracy)
